#include <bits/stdc++.h>

#define FOR(i,a,b) for(int i=(a);i<(b);++i)

template<typename T>
void bfs( std::vector<std::vector<T> > graph, T start_node, std::vector<T> visited_nodes );

template<typename T>
void dfs( std::vector<std::vector<T> > graph, T start_node, std::vector<T> visited_nodes );
